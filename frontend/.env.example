# Backend REST API base URL
VITE_API_URL=http://localhost:8000

# Backend WebSocket base URL (must match API URL but with ws:// or wss:// scheme)
VITE_WS_URL=ws://localhost:8000

# Comma-separated allowed origins for the backend CORS config (informational only)
VITE_FRONTEND_ORIGIN=http://localhost:5173
